export function Trail({ count, current }: { count: number; current: number }) { return <div className="trail" role="progressbar" aria-valuemin={1} aria-valuemax={count} aria-valuenow={current + 1}>{Array.from({ length: count }, (_, i) => <span className={`trail-node ${i < current ? 'complete' : ''} ${i === current ? 'current' : ''}`} key={i} />)}</div>; }
